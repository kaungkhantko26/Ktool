#!/usr/bin/env sh
SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
if [ "${KTOOL_AUTO_DEPLOY:-0}" = "1" ] && [ -x "$SCRIPT_DIR/deploy.sh" ]; then
  "$SCRIPT_DIR/deploy.sh" "Auto deploy Ktool updates" || true
fi
exec python3 "$SCRIPT_DIR/tool.py" Ktool "$@"
